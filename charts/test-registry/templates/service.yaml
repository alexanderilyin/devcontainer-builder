apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-registry.fullname" . }}
  labels:
    {{- include "test-registry.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "test-registry.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
