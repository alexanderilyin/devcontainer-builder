apiVersion: v2
name: nginx
description: >-
  Minimal stock-nginx deployment for exercising the fire-and-forget /
  selector-alias BDD step patterns against a real cluster. Not a production
  chart - no persistence, no config customization, just the standard image
  behind a ClusterIP Service.
type: application
version: 0.1.0
appVersion: "1.27"
